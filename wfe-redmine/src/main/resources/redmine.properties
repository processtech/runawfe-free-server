# Override via REDMINE_URL / REDMINE_API_KEY environment variables (preferred for secrets)
# or set the values here for local development. Do NOT commit real API keys.
redmine.url=http://localhost:8081
redmine.apiKey=
